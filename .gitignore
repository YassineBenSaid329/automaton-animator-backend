# --- Virtual Environment ---
# Never commit the virtual environment folder. It's machine-specific.
# The requirements.txt file will be used to recreate it instead.
venv/
.venv/

# --- Python Cache & Compiled Files ---
# Ignore Python's bytecode files and cache folders.
# These are auto-generated during execution.
__pycache__/
*.pyc
*.pyo
*.pyd

# --- Testing & Coverage ---
# Ignore caches and reports from testing tools. These are generated
# during test runs and should not be version controlled.
.pytest_cache/
.hypothesis/
.coverage
htmlcov/

# --- IDE & Editor Directories ---
# Ignore configuration files from popular editors like VSCode, PyCharm, etc.
# These are user-specific preferences.
.vscode/
.idea/

# --- OS-generated files ---
# Ignore metadata files generated by macOS or Windows.
.DS_Store
Thumbs.db

# --- Secrets & Environment Variables ---
# Ignore files containing sensitive data. A .env.example file is often
# committed instead as a template for other developers.
.env
*.env

# --- Docker ---
# Ignore local overrides for Docker Compose. The main docker-compose.yml
# should be committed, but developer-specific overrides should not.
docker-compose.override.yml
docker-compose.local.yml